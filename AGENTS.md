# Dish Discovery development

This checkout is independent of the original Lovable project. Changes are stored in the Sites source repository and are not synced back to Lovable.

Preserve the user's white/gray mobile-first design and bottom navigation. Keep only member premium plans; all reviewers are ordinary users; advertising is through AdSense, not direct advertising plans.

Run the backend tests, TypeScript check, and production build for changes to account state, billing or persistence. Never enable live payments or AdSense without the required provider setup. Keep secrets out of committed files. Preserve applied Drizzle migrations and append new migrations for subsequent changes.
